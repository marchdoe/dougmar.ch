// shell_posture: folded-into-hero — nav and identity live inside each page's
// hero composition (see Masthead in app/components/generated/), not in a
// persistent sidebar slot. This component intentionally renders nothing.
export function Sidebar() {
  return null
}
