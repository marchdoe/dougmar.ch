// shell_posture: none — the composition carries no rail/nav chrome.
// Sidebar exists to satisfy the shell contract but renders nothing:
// wayfinding lives as in-content links inside each page's BUILT column.
export function Sidebar() {
  return null
}
